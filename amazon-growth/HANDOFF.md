# Codex / Cursor 协作状态

更新时间：2026-09-14

## Codex 换号接力（2026-09-14）

- 正式仓库：`D:\systemDir\Desktop\Hubstudio\新建环境3\ops-center-release`，GitHub `xiaopong190-oss/ops-center`，`main` 已与远端同步；最近发布提交为 `80c5f18`。
- 本地入口：主平台 `http://127.0.0.1:5174/`；自包含正式版开发入口 `http://127.0.0.1:5175/`；正式版 Amazon Growth 服务 `http://127.0.0.1:8791/`。
- 已完成：一级菜单“亚马逊增长”及运营调用、完整竞品、监控清单、推品计划、关键词库框架；各页面有使用说明；MCP/LLM 管理入口只在超级管理员界面显示。
- 卖家精灵：正式版连接组已有“卖家精灵（原关键词平台）”，实测连接成功，识别 44 个接口。密钥只在 `proxy/connections.private.json` 加密保存，该文件被 Git 忽略，禁止提交或打印。
- 旧平台 `http://127.0.0.1:8787/` 的连接组已确认存在：西柚 A、西柚 B、西柚 C、卖家精灵 A；3 个西柚均指向 `mcp.xydc.com`，都有 Bearer 密钥。旧加密文件受 Windows DPAPI 当前用户保护，当前 Codex 执行身份无法直接解密，但旧运行进程仍可调用。
- 西柚待办：把旧平台的西柚 A/B/C 安全迁移到正式版管理员连接组并逐一执行 `tools/list` 测试。不要要求用户重新整理连接；优先在原 Windows 登录身份下转存，且不得把 Token 写入源码、日志、Git 或聊天。
- LLM：前端默认模型名为 `claude-sonnet-4-6`，服务端目前只实现 Anthropic Messages API；因 `ANTHROPIC_API_KEY` 未配置，当前实际没有可调用的 LLM。Cloudflare AI 与通用 OpenAI-compatible LLM 尚未接线。
- 权限边界：当前已实现超级管理员的前端可见性与点击保护；上线前仍应把连接管理 API 从 `X-Connection-Admin: 1` 升级成服务端会话/RBAC 校验，不要把现状描述成生产级安全鉴权。
- 接手第一步：先读本节，再请求 `GET http://127.0.0.1:8787/api/connections` 与 `GET http://127.0.0.1:8791/api/connections`（管理 Header）核对状态；不要覆盖已有正式版卖家精灵连接。

Cursor 每次开工先读本文件，做完后只改「最近完成 / 验证结果 / 下一步」。Codex 验收时也只读本文件和点名文件，不要重新扫整个项目。

## 分工

- Cursor：常规开发、样式、批量改、跑 `npm test`、把进度写回本文件。
- Codex：分析逻辑、接口组合、疑难排错、最终验收。只看本轮变更和失败日志。

## 当前基线

- 页面：`http://localhost:8787/` 西柚运营调用台；页头切换 `http://localhost:8787/asinscope/` 完整竞品分析、`http://localhost:8787/watchlist/` 监控清单
- 启动：`npm start` 或 Tasks → 启动亚马逊运营调用台（同一进程同时监听 8787 和 3210）
- 验证：`npm test` 或 Tasks → 运行完整测试
- 接口目录：44 个西柚接口 + 19 个西柚缺失的卖家精灵接口，共 63 个
- 交叉验证：另有 5 个卖家精灵接口，不计入 19 个缺失接口
- 作业流：31 条，分为 5 个模块
- 数据持久化：工作流结果写入 IndexedDB，刷新后自动恢复
- 报告策略：优先本地规则生成结果，外部模型只做可选增强
- 密钥：只放本机 `.env` 或页面连接设置；`proxy/connections.private.json` 不进压缩包、不进规则

## 最近完成

- 监控清单：空结果不再堆进「取数失败」卡片，改成可展开的「空结果 / 接口报错」。窗口必填、最多一个月（去掉近 90 天，超 31 天收到结束日前）。新增「保存本期数据到清单」勾选，不勾只出报告不占档案。未动 asinscope。硬刷新 `?v=20260912-wl7`。
- 监控清单报告改成主 ASIN 每日定点档案，不是九章竞品分析。五章：每日定点 / 主动与被动 / 周期有效动作 / 可学习与不可复制 / 对照（可选只计数）。动作后 3–7 日叶子 BSR 改善才算有效；改 Listing、降价、优惠可学，广告只核验不复制花费。日表「优惠」是当天有没有券；主动「优惠」只算出券（从无到有）。未动 asinscope。硬刷新 `?v=20260912-wl6`。
- 监控清单改为完成档案：只保存已跑完的期报快照，不再把空参数当清单。跑完自动写入；切换清单打开最近一期；任务历史按本组各期回看。硬刷新 `?v=20260912-wl4`。
- 监控清单写清两件事：本页控制名单/站点/窗口/核心词，≥7 天出周期报告且不会自动出报；同一份清单同时对照最多 8 个 ASIN，换一组就另存清单。补回上次测试 4 个亚马逊链接和「填入测试名单」，并加近 7/30/90 天窗口。硬刷新 `?v=20260912-wl3`。
- 调用台左栏去掉「监控清单已独立到 /watchlist/」提示条。页头仍可切到监控清单。硬刷新首页即可。
- 监控清单会带上调用台旧数据：每次打开合并 `xiyou-console-v1` 清单/当前表单、`asinscope-session-v1` 当前表单，并读取 IndexedDB `xiyou-console-cache-v1` 的 ok 步骤（含 `targetAsin`）。已有槽位不再当失败、不重打 MCP；后台 `POST /api/evidence/publish` 把旧 payload 填进网关。硬刷新 `?v=20260912-wl2`。
- 监控清单已从调用台左栏拆成独立页 `http://localhost:8787/watchlist/`。每个 ASIN 拉西柚监控相关接口，并拉卖家精灵 Keepa/优惠/销量/出单词/评论/竞品/关联流量；有核心词再拉词位。调用台旧清单会自动接手。硬刷新 `?v=20260912-wl1`，并重启 `npm start` 才能挂上新路由。
- 完整竞品分析已套用调用台同一套 UI：页头（主题 / 连接条 / 切换）、左栏参数、右栏作业区、分析报告/任务历史页签、配置弹层。分析逻辑、权重和历史仍只留在本页。硬刷新 `?v=20260912-ui1`。
- 05「数据不足」：西柚价格在 `trends[].priceDistribution.display`，改版在 `previous/current.title|image`，广告在 `added` 活动，不是顶层 price / 88 条快照。已按这些字段拆日点、真改版和新增广告。硬刷新 `?v=20260912-s5px` 后用缓存重跑即可。
- 完整竞品分析九章都按 VOC 同款：导语 + 核心结论 + 分析过程步骤 1–5 + 表 + 边界。01 拆标题功能不贴整段英文；02 叶子 BSR 阶段、点数不当趋势；03 词条数不是护城河、占比分摊不是归因；04 先看 BSR 再谈驱动，走平不当爆发；05 日快照不当改版、次数不当打法；06 子体名单不是走量、主推未定；08 缺字段不补零；09 只核验已支持动作。硬刷新 `?v=20260912-ch9` 后开「仅用已有数据」重跑。旧报告不会自己长出过程。
- 完整竞品分析 07 VOC 不再只报「评论样本 10」。已把卖家精灵评论正文做痛点分层：分析过程步骤 1–5、核心结论、分层表、主题次数、摘录。条数不当结论，词典未命中不编造。标准模式仍是混页最多 10 条。硬刷新 `?v=20260912-voc` 后开「仅用已有数据」重跑即可，不必为评论再打接口。
- 生命周期「数据不足数据不足」：西柚 `get_asin_bsr_trends` 是 `trends[].values[].rank` + `categoryTree`，不是顶层 `rank`。已按叶子类目拆日点，卡片写增长/衰退/成熟和首末名次。硬刷新 `?v=20260912-bsr` 后可用缓存重跑，不必为 BSR 再打新接口。
- 竞品分析：点「开始分析」后按钮变橙红、写「分析中 n/n · 接口名」，额度条同步变色，禁止连点。西柚变体空结果不再挡住 Keepa 子体。硬刷新 `?v=20260912-runui` 后重跑，旧报告时间停在 01:35 的不会自己补数。
- 完整竞品分析：西柚空结果或没有的接口改补卖家精灵。标准模式必拉 `review`；变体/BSR/价格补 `keepa_info`，订单补 `asin_sales_trend`，词补 `keyword_order`，详情/优惠补 `asin_detail_with_coupon_trend`。日快照不再计成 Listing/广告改版次数。额度条会写「西柚空结果最多再补卖家精灵 N」。硬刷新 `?v=20260912-ssfb` 后重跑。
- 完整竞品分析 19 步全空：西柚 `get_asin_info` 等接口必填是 `country`，网关只传了 `marketplace`/`site`，证据库 22 次错误、0 次成功。现按 schema 补 `country`/`country_code`，丢掉 schema 没有的字段，月度接口把日期收成 `start_month`。报告会列出失败步骤，不再只闪一下 toast。硬刷新 `?v=20260912-country` 后需重新点「开始分析」。
- 竞品分析缺 `asins` 已修：请求同时带 `asin` 和 `asins[]`，网关按 schema 把单个 ASIN 补成数组。单步 400 不再整场中断。
- 竞品分析页会把草稿和最近报告写入 `asinscope-session-v1` / IndexedDB，页头切回调用台再回来不会丢 ASIN、窗口和报告。调用台离开前也会再存一遍表单。
- 页头可在「运营调用台 / 完整竞品分析」之间切换。竞品页挂到 `http://localhost:8787/asinscope/`，不嵌 iframe，不共用表单和任务历史。中文优先用微软雅黑，去掉会把页头挤花的 Google 字体。
- 共享 MCP 网关第一阶段已落地：`gateway/` 统一连接、证据缓存、TTL、并发去重、额度统计和 402/429 备用切换。`npm start` 同时开 8787 与 3210。8787 的 `pool.js` 先走 `POST /api/evidence/call`；3210 的 `asinscope/assets/mcp-client.js` 只打网关，不存 Token。两边 localStorage 键分离（`xiyou-*` / `asinscope-*`）。网关按 schema 把卖家精灵扁平参数包进 `request`。
- 3210 已有独立完整竞品页：目标/对比 ASIN、90 天窗口、快/标/深、分析前额度估算、仅用缓存、本工具权重、六张结论卡、九章节和本机任务历史。刷新 8787 不会丢掉 3210 报告。
- 已按用户最终边界修正完整竞品方案，见 `COMPETITOR-WORKSPACE.md`：8787 西柚运营调用台与 3210 ASINSCOPE 保持两个独立 UI、输入、任务和报告；底层通过本机 `mcp-evidence-gateway` 共用同一连接池、加密 Token、调用缓存和额度审计。只共享标准化接口证据，不共享页面状态或自定义结论。
- 已把 `asinscope-app` 的确定性爆发分析整合进原 s9，并把场景升级为“竞品爆发归因”：新增 BSR 趋势与变体结构证据；使用至少 14 个有效点、7 日因果平滑、3–7 日异常窗口、2.5σ、1.4× 改善和 14 日持续性检测；评论/变体倍数跳变会作为伪爆发排除。报告输出广告、关键词、降价、Listing 的支持/不支持/无法验证矩阵及相对权重。旧流量算法保留为 BSR 点数不足时的降级路径。
- UI/UX 检测后已落地首轮优化：压矮页头并把连接状态做成可点入口；按场景只显示相关参数（可勾选「全部参数」）；场景/接口可搜索并按模块筛选；卡片不再堆接口链；步骤返回默认折叠；「生成分析报告」提到运行区顶部；取数纪律收进折叠；补深浅色切换；中文字体改为系统字体优先，不再依赖 Google CJK。
- 已去掉「商标与命名风险初筛」（原 s32）。作业流现为 31 条。商标四个接口仍留在接口速查，不再单独成场景。
- 「销量预测三角校验」不再数成功步数。历史读 `salesTrendPoints`（子体优先），ASIN 预测读 `monthItemList`，BSR 预测要用大类 `bsrRank` + `nodeIdPath` 第一段，不能把类目文本或西柚 categoryId 传进去。报告写三条路径、重叠偏差和置信，明确不能当首批产量。旧的 2/3 覆盖页需重跑。
- 「VOC痛点与功能机会」不再数「5 条记录」。评论接口默认 5 条、最多 10 条。现按 1–2★ / 3–4★ / 5★ 分批拉正文，报告分层（可改进/差评/物流/期望/功能），主题次数带证据等级。旧的 5 条混页可生成摘录，但要重跑才能按星级补样本。
- 「市场进入壁垒全检」不再用记录条数冒充壁垒。市场接口只要数字 `nodeIdPath`，类目文本 `electric kettle` 会让集中度空返回。现先跑 `product_node` 选 Electric Kettles 叶子，有主 ASIN 时用详情路径覆盖，再拉六维份额。报告写 CR、Amazon 自营、国家、年龄、A+，不要点旧结果「生成报告」。硬刷新后选美国站并重新运行。
- 「关联流量关系图谱」明细按类型分次拉：看了又看 / 组合购买 FBT / SP 等，表上「来源」列写清来自哪个版块。旧的混页结果点生成报告会写「未分类型」，需重新运行。
- 「关联流量关系图谱」`traffic_listing` 缺必填 `relations`（及 `asinList`）。已按统计非零类型补上，默认带 vav/sp 等。统计表按条数排序，避免 mib/fbt=0 挡在前面。硬刷新后点「只重试失败步骤」即可拉明细。
- 美国站词表里的 `keywordJp` 是英文词的日文译名，不是日本站。表格改为优先显示 marketplace / 英文词 / 中文译名，美国站不再把日文列顶出来，并加一句说明。
- 美国站 ASIN 被卖家精灵落到日本站：Keepa/详情不带 `marketplace` 会默认 JP，同一 ASIN 在日本也有占位页。现在只要工具需要站点就强制带上表单站点（空则 US）。报告会写清美国站 / amazon.com；若返回 amazon.co.jp 会提示整场重跑。美国站价格符号 `Y` 仍按美元读，不是日元。硬刷新后重跑「竞品发现与历史全景」。
- 浏览器请求 `/favicon.ico` 时代理不再返回 JSON「禁止访问」；首页和脚本本身本来就可以访问。
- 卖家精灵 MCP 已按真实 `tools/list` 接上：参数打进必填对象 `request`。`keyword_miner` / `google_trend` 传 marketplace + keyword；`keyword_order` 传 `asins[]`、`reverseType=M`、上一完整月 `yyyyMM`。脚本加了 `?v=20260911-s27`，避免继续用旧缓存。实测三步均返回 `code=OK`。
- 参数栏关键词改为可填多个：逗号、分号或换行分隔，最多 8 个，词面空格保留（`electric kettle, gooseneck kettle` 是两个词）。需要关键词的接口会按词分别请求，报告按词对照。
- 单词效果复盘（s22）：流量读 `summaryTraffic.organic / advertising`（日期可以是 `{startDate,endDate}`），自然位读 `displayPositions` 里 `displayPosition=or` 的 `totalRank`。自然流量整天是 0 仍画出广告曲线，不再写「不能复盘 / 没有可比点」。同期修了卡位追踪（s16）同一套字段。
- 上词掉词诊断（s19）：日级日期读 date / localDate / ranks.rankTime；数量读 organicKeywordCount 或 keywordCount.organic。日级不够两个日期时改用月度首末月。卡片和结论不再写「数据不足」。
- 竞品词位反打（s18）：西柚自然位在 `ranks[]` 里，`position=or` 的 `totalRank` 才是自然位，不是 `organicRank`。按这个字段分组可抢/前排；只有广告位没有 or 才标无自然位。即使 20 个词都在前 4 位，也列出完整词表。反查词返回后最多补 5 个 `get_keyword_info` 和 2 个 `get_keyword_asin_analysis`。
- 关键词赛道格局（s17）「数据不足」：短窗口时月度接口自动拉到 6 个月；报告写清为什么不够（月份/ABA 周点/表单天数）以及要怎么补。ABA 优先读周趋势，不再被头部 ASIN 列表挡住。日期提示 ≥90 天。
- 场景必填参数改回高对比：选中场景后，必填表单加红框、「必填」标记和深色字；场景卡片与运行区用实心红底白字标签。空着或格式不对时再叠一层 miss。
- 广告词库搭建（s15）改成可投放分组词库：给出投放结构、每组词清单（词/来源/搜索量/难度/CPC）、分组计算过程。否定词和指标不足单独成组，不再只报词数。类目词返回后最多给 5 个核心候选补 `get_keyword_info`。
- 广告位竞争侦察（s14）改成按位置格读放映机：每一格标明是单个广告位、广告区还是整页广告集合，并写出位置类型、页码、页内位。最松窗口按同一个格比广告条数，不再把首页广告加总说成某一个位空了。常客可对上 `get_keyword_asin_analysis` 的点击/转化份额。
- 修复 `get_keyword_aba_trends` 缺少必填 `start_week`：用表单日期换算成周一起始（或 schema 要求的 ISO 周），并同时补 `end_week`。
- 每日监控快照（s23）改成监控名单周期报表：主 ASIN + 对比 ASIN（最多 8 个）对照改版、订单、流量结构；其余竞品补拉 Listing/新广告。输出名单、各竞品打法、交叉结论和下一周期盯谁。不是当天临时看一眼。
- 修复报告生成失败 `Cannot read properties of null (reading 'label')`：趋势只有 1 个点时不再读空的 first.label；接口 list 里的空行会跳过。
- 竞品改版监控（s11）改成周期档案：按周/月汇总 Listing、价格动作、新广告，用反复次数下打法结论（反复促销冲量 / 持续测 Listing / 持续加投），并给出下一周期盯什么。不再输出当天跟不跟。日期建议 ≥90 天。价格只在相邻日 |Δ|≥5% 记动作。
- 流量结构拆解（s8）按目标补齐：用流量占比分摊近 30 天推算订单（自然/广告单量）、拆 OR/SP/SB/SBV 坑位、判断 SB/SBV 是否撑量、周配比稳态，以及能不能复制。这是分摊不是后台归因。
- 31 个场景全部改成专业分析页：必须有「分析过程 / 步骤 1」+ 核心结论。卖家精灵 s26–s31 各有场景步骤。
- 类目黑马产品雷达（s24）已做成与集中度同级的专业分析页：三榜交叉、新品榜完整性（非官方 HNR）、成熟 Ratings 门槛、共享评价清洗、高置信黑马、价格带、突破类型与第一驱动、成活率边界。跑 s24 时会像新品扫描一样，对近 180 天出量新品补 `get_asin_traffic`。

- 品牌格局与集中度（s5）已加上四条判断，并写入 `.cursor/rules/project.mdc`：
  1. 细分搜索量：近 90 天词表均值比近 360 天高 5% 以上算在涨；同期商品数量变化 ±10% 内算没怎么变。同时成立才点亮黄金信号。商品数量用机会接口新品/代表父体口径，不是全类目在架 SKU。
  2. 搜索转化集中度：细分词竞争 ASIN 转化/购买份额前三相加。≥80% 垄断严重；>60% 新手回避；40%–60% 有切入空间。不得用点击份额冒充转化。
  3. 价格带：销量最大的低/中/高档视为主流。主 ASIN 落在带外且相对中位偏离 >30%，必须先有明确差异化理由。
  4. 点击 vs 购买：点击前三 <60% 记分散，购买/转化前三 ≥60% 记集中。点击分散但购买集中 = 比较后买头部；两者都 <60% = 蓝海。
- s5 链路在品牌规模/趋势后追加：`get_category_keywords`、`get_category_new_release_opportunity_trends`、`get_category_price_segment_trends`、`get_asin_info`（无主 ASIN 则跳过）、`get_keyword_asin_analysis`（无关键词则跳过）。
- 缺字段写「数据不足」，图表缺列不画、不补零。
- 增加本交接文件、Cursor 规则、启动任务和测试任务。

## 验证结果

- 监控清单窗口夹具：30 天不裁切，超过一个月裁成 31 天，缺日期报错。空结果与报错分开。硬刷新 `?v=20260912-wl7`。
- 监控清单日记夹具：改 Listing + 降价后 BSR 改善记「可学习」；仅新广告即使名次改善记「可核验名次，不可复制花费」；连续有券不当新动作。空名单写「没有主 ASIN」。本机仅用已有数据重跑 B0GH78L65N：五章已出，定点 72 天，BSR 82→54，主动动作从 71 天压到 2 天，可学习 1 条。未改 asinscope。硬刷新 `?v=20260912-wl6`。
- 监控清单不再是空设定：没有期报不能「存为」。跑完写入快照，下拉显示「n 期 · 日期」。硬刷新 `?v=20260912-wl4`。
- 调用台左栏已去掉独立页提示条；参数区从站点字段开始。页头「监控清单」仍在。
- 监控清单带旧数据：打开即合并调用台清单 + IndexedDB ok 步骤；本机应出现「调用台当前 / 调用台 · 每日监控快照」并直接出周期报告，不必再打已有接口。硬刷新 `?v=20260912-wl2`。`npm test` 含 ingestSteps（只收 ok、认 targetAsin、跳过 error）。
- 监控清单独立页：本机已重启代理。`/watchlist/?v=20260912-wl1` 接手了调用台旧清单「Stariver goose neck」；仅用缓存跑完后 01 为「高」，名单 4 个，订单最高 B0BNVHR71X。调用台左栏已无存为/导入。`npm test` 101 + watchlist 通过。
- 竞品页 UI 已与调用台对齐：本机 `?v=20260912-ui1` 左栏参数、右栏作业区、主题自动/浅/深、配置弹层、报告/历史页签可用。缓存报告仍在，05 仍为「高」。`npm test` 通过。硬刷新后才会换壳。
- 05 字段修复：`priceDistribution.display` / `previous.current` / `added` 夹具通过。本机 `?v=20260912-s5px` 缓存重跑后 05 为「高」：88 个价格日点、8 次 ≥5% 动作、1 次标题改版、6 条新广告、32 个优惠日。旧报告必须重跑。
- asinscope 九章夹具：每章都有步骤 1 和至少 2 条核心结论；空底稿不编定位；无对比 ASIN 不写成蓝海。只有 changeType 的日快照不再计 Listing 改版。`npm test` 通过。
- asinscope VOC 夹具：漏水 3–4★ 会出主痛点与可改进分层；无正文写数据不足；5★ 无词典命中不编造漏水。本机硬刷新 `?v=20260912-voc` 后「仅用已有数据」重跑，07 已出现分析过程步骤 1–5、核心结论、分层/主题/摘录；B0BNVHR71X 10 条混页无立项痛点，功能侧鹅颈/手冲 6 次。旧报告只存了条数，必须重跑才能看到过程/结论。
- 本机 `POST /api/evidence/call` 复现：`missing required argument "country"`（缓存 0、西柚 errors 22）。补 `country` 后同一 ASIN 的 `get_asin_info` 返回 200，`data.entities` 含 title/price/stars/ratings。网关测试覆盖 country 别名与多余字段剥离。
- `npm test`：通过（adversarial 99 + connections + local-report + visual-report + gateway + asinscope；含西柚空结果补卖家精灵、标准模式拉评论、日快照不计改版）。
- 网关：8787 与 3210 共用 `/api/evidence/*`；恶意 Origin 被拒；响应经 `publicSafe` 剥密钥。
- 本机核对：8787 首页页头可切到 `/asinscope/`；竞品页不加载 `scene-reports.js`。脚本缓存 `?v=20260912-switch`。
- `asinscope-app` 爆发归因迁移回归：通过（可信 BSR 拐点、BSR 模式、驱动矩阵、权重与持续性）。
- UI/UX：本机 `http://localhost:8787/` 已核对页头压缩、按场景收参数、场景搜索「竞品」、接口搜索「流量」、连接条打开设置、外观在自动/浅色/深色间切换。脚本缓存版本 `?v=20260912-ux`。
- 卖家精灵实调：`keyword_miner` / `keyword_order` / `google_trend` 均 `code=OK`。
- 只点「生成分析报告」不会补齐未跑过的新接口。s5 四条信号要用户重新跑该场景后才有数据。

## 关键文件

- `HANDOFF.md`：协作状态（本文件）
- `COMPETITOR-WORKSPACE.md`：双界面边界与网关方案
- `gateway/evidence.js`、`gateway/caller.js`、`gateway/http.js`：共享证据仓库、MCP 调用、CORS/API
- `asinscope/`：3210 独立 UI、`mcp-client`、分析规则
- `tests/gateway.cjs`：网关隔离、去重、TTL、密钥、故障切换
- `CURSOR.md`：Cursor 打开方式和首条指令
- `.cursor/rules/project.mdc`：始终生效的项目约定
- `.vscode/tasks.json`：启动调用台、跑完整测试
- `assets/data.js`：接口、模块、场景和步骤
- `assets/mcp.js`：MCP schema 参数映射和响应处理
- `assets/app.js`：运行编排、跳过原因、持久化和界面
- `assets/scene-reports.js`：`brandReport`（s5）等专业本地报告
- `assets/report.js`：`ACTIONS.s5` 行动建议
- `tests/visual-report.cjs`：s5 品牌结构 + 黄金信号/转化/价格/点击购买夹具

## 协作约定

1. 开始前读本文件，只打开与当前任务直接相关的文件。
2. 每次只把本轮变更和失败日志交给 Codex，避免重新扫描整个项目。
3. 修改完成后更新「最近完成」「验证结果」「下一步」。
4. 不删除旧分析结果；重新调用 MCP 由用户主动运行场景。
5. 不把 Token、`.env`、`connections.private.json` 写入源码、规则或交接包。

## 下一步

- 硬刷新 `http://localhost:8787/watchlist/?v=20260912-wl7`。日期最多一个月；空结果在卡片下可展开，不再撑破「取数失败」。勾选「保存本期数据」才会写入清单。不要改 asinscope。
- 硬刷新 `http://localhost:8787/`，左栏不应再出现「监控清单已独立」提示。监控清单仍从页头进入。
- 用户硬刷新 `http://localhost:8787/asinscope/?v=20260912-ui1`，核对页头、左栏参数、右栏作业区和浅/深色是否与调用台一致。旧缓存脚本不会自己换壳。
- 05 已在本机缓存重跑验证为「高」。用户若仍看到「数据不足」，硬刷新后开「仅用已有数据」再点「开始分析」。旧报告不会自己更新。
- 用户硬刷新 `http://localhost:8787/asinscope/?v=20260912-bsr` 后点「开始分析」或「仅用已有数据」。BSR 已在缓存里，生命周期应出现叶子名次而不是双「数据不足」。
- 重启 `npm start` 后分别打开 `http://localhost:8787/` 与 `http://localhost:3210/`，确认两边刷新/清空互不影响；同一 ASIN 的 BSR 等证据应能互相命中缓存。旧进程没有 3210 监听。
- 8787 旧 IndexedDB 结果「发布到共享证据库」的页面入口尚未做；接口 `POST /api/evidence/publish` 已就绪。
- 3210 章节仍是独立规则摘要，不是 8787 `scene-reports.js` 的移植；深度模式图表与 VOC 分层可继续加厚，但不要把 3210 权重写回 8787。
- 用户硬刷新后重新运行“竞品爆发归因”。旧 s9 结果没有新增的 BSR 与变体步骤，只能显示流量降级报告；建议日期窗口至少 30 天，理想为 60–90 天。
- 硬刷新后确认新界面：页头变矮、左侧只显示当前场景相关参数、场景列表可搜索。旧缓存脚本是 `?v=20260911-s32off`，需硬刷新到 `?v=20260912-ux`。
- 硬刷新后确认站点是 **US**，保留主 ASIN，**重新运行**「销量预测三角校验」。旧的「成功 2/3、记录 2 条」不是三角校验；BSR 步必须带着大类排名和一级类目节点。
- 硬刷新后确认站点是 **US**，保留主 ASIN，**重新运行**「VOC痛点与功能机会」。旧的「5 条结构化记录」页可点生成报告看摘录，但只有重跑才会按星级分批补样本。
- 硬刷新后确认站点是 **US**，类目写 electric kettle，保留主 ASIN，**重新运行**「市场进入壁垒全检」。旧的 6/6/18 条覆盖图不能点生成报告，那是空路径记录数。
- 硬刷新后**重新运行**「关联流量关系图谱」，导流表会出现「来源」列（看了又看 / 组合购买 FBT）。旧混页明细只点生成报告不够。
- 硬刷新后确认参数栏站点是 **US**，重新跑「竞品发现与历史全景」。旧 IndexedDB 若 Keepa 是 amazon.co.jp，不能点生成报告当美国站用。
- 用户需硬刷新后，在参数里用逗号或换行填多个关键词（最多 8 个），再跑需要关键词的场景；旧结果点生成报告不会按新词补接口。
- 用户需硬刷新后重新打开「单词效果复盘」点生成报告即可，不必重跑接口。流量读 summaryTraffic，自然位读 displayPositions.or.totalRank；自然流量整天是 0 也会画出广告曲线。
- 用户需硬刷新后重新打开「上词掉词诊断」报告（旧结果可直接生成，不必重跑）才能看到新增/掉出词和词数量曲线；卡片不再写「数据不足」。
- 用户需硬刷新后重新跑「竞品词位反打」，才能看到 20 个竞品词的完整分组表；旧结果点「生成分析报告」可换模板，但不会补拉新的词信息/竞争名单。
- 用户需把「关键词赛道格局」起始日期提前到至少 90 天（建议 6 个月）后重新跑，才能看到搜索量涨跌和新进入者；旧的一个月结果不够判断格局。
- 用户需重新跑「广告位竞争侦察」，才能看到每个位置格是一个位还是一页、以及按位计算的最松时段。
- 用户需把「每日监控快照」的对比 ASIN 填成监控名单、日期拉到 ≥7 天后重新跑，才能看到竞品周期对照。
- 用户需把「竞品改版监控」日期拉到 90 天或更长后重新跑，才能看到长期打法汇总；短窗口只写近期节奏。
- 用户需重新跑各场景才能看到新专业分析页；旧 IndexedDB 结果点「生成分析报告」可换模板，但不会补未跑过的接口。
- 用户需重新跑「类目黑马产品雷达」，才能看到高置信黑马、完整性核验和第一驱动。
- 用户需重新跑「品牌格局与集中度」，才能看到黄金信号、转化前三、价格偏离、点击 vs 购买。
- 新需求优先扩展现有 31 个场景，避免创建功能重复的场景。
- 卖家精灵出现参数错误时，先读取 `tools/list` schema，再调整 `assets/mcp.js`。
